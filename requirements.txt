# pip install -r requirements.txt

# Core dependencies
numpy==1.23.5             # Numerical computations
pandas==1.5.3             # Data manipulation
sqlite3                   # Database (built-in with Python)

# MQTT for real-time sensor communication
paho-mqtt==1.6.1

# Flask + Dash for the Web Dashboard
flask==2.2.5
dash==2.15.0
dash-bootstrap-components==1.4.1

# Email notifications
smtplib                   # Email sending (built-in with Python)

# Data Visualization (Optional)
plotly==5.11.0
